<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to "Home", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <div class="navbar-nav me-auto mb-2 mb-lg-0">
        <span>Welcome<%= ", #{current_user&.first_name}" if current_user.present? %>!</span>
      </div>
      <form class="d-flex">
        <% if current_user %>
          <%= link_to "Sign Out", destroy_user_session_path, data: { turbo_method: :delete }, class: "btn border-secondary" %>
        <% else %>
          <%= link_to "Sign In", "#", { remote: true, "data-bs-toggle" => "modal", "data-bs-target" => "#sign-in-modal", class: "btn border-primary mx-2" }  %>
          <%= link_to "Sign Up", "#", { remote: true, "data-bs-toggle" => "modal", "data-bs-target" => "#sign-up-modal", class: "btn border-info mx-2" }  %>
        <% end %>
      </form>
    </div>
  </div>
</nav>
