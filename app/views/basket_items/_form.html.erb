<%= form_with(model: basket_item) do |form| %>
  <% if basket_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(basket_item.errors.count, "error") %> prohibited this basket_item from being saved:</h2>
      <ul>
        <% basket_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :basket_id, style: "display: block" %>
    <%= form.collection_select(:basket_id, baskets, :id, :name) %>
  </div>

  <div>
    <%= form.label :item_id, style: "display: block" %>
    <%= form.collection_select(:item_id, items, :id, :name) %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :note, style: "display: block" %>
    <%= form.text_field :note %>
  </div>

  <div>
    <%= form.label :quantity, style: "display: block" %>
    <%= form.text_field :quantity %>
  </div>

  <div>
    <%= form.label :measure, style: "display: block" %>
    <%= form.text_field :measure %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
